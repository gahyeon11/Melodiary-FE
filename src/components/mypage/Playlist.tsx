import styled from 'styled-components';

const Playlist = () => {
  return (
    <PlaylistWrapper>
      <h1>Playlist</h1>
    </PlaylistWrapper>
  )
};

const PlaylistWrapper = styled.div``;

export default Playlist;