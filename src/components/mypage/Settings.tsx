import styled from 'styled-components';

const Settings = () => {
  return (
    <SettingsWrapper>
      <h1>Settings</h1>
    </SettingsWrapper>
  )
};

const SettingsWrapper = styled.div``;

export default Settings;